# Dir navigation
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'
alias ~='cd ~'
alias ll='tree --dirsfirst -ChFupDaLg 1'

# Utilities
alias a="ack -ia"
alias b="bundle exec"
alias c="pygmentize -O style=monokai -f console256 -g"
alias d="du -h -d=1"
alias df="df -h"
alias g="git"
alias grep='GREP_COLOR="1;37;45" LANG=C grep --color=auto'
alias h="history"
alias httpdump="sudo tcpdump -i en1 -n -s 0 -w - | grep -a -o -E \"Host\: .*|GET \/.*\""
alias ip="curl -s http://checkip.dyndns.com/ | sed 's/[^0-9\.]//g'"
alias localip="ipconfig getifaddr en1"
alias mp="mvim -p"
alias rkt="racket -il xrepl"
alias view="vim -p -R"
alias vp="vim -p"

# J = Z
alias j="z"

# t
alias todo="subl ~/Dropbox/todo.txt"
alias t="todo.sh"
alias ts='t | grep -v LongTerm'
# Enable alias completion
complete -F _todo t

# Git Repos
function gg() {
  if [ $# -gt 0 ]; then
    for i in $(echo $*); do
      if [ -d ${i}/.git ]; then
        ( echo '*** Pulling' ${i} && cd ${i} && git pull | grep -v "Already up-to-date." )
        ( cd ${i} && git cherry -v origin && git status -s )
      fi
    done
  else
    echo "usage: gg <git repo dir>"
  fi
}

function gs() {
  if [ $# -gt 0 ]; then
    for i in $(echo $*); do
      if [ -d ${i}/.git ]; then
        ( echo '***' ${i} && cd ${i} && git cherry -v origin && git status -s )
      fi
    done
  else
    echo "usage: gst <git repo dir>"
  fi
}

function gz() {
  if [ $# -gt 0 ]; then
    for i in $(echo $*); do
      if [ -d ${i} ]; then
        ( echo "*** Tarring into ~/Dropbox/backups/ -- " ${i} " -- ***" && tar zvcf ~/Dropbox/backups/$(date +"%F").${i}.tgz ${i} )
      fi
    done
  else
    echo "usage: gz <dir name>"
  fi
}

# function ascifix() {
#   find . -type f | xargs perl -pi -e 's/\“/\"/g';
#   find . -type f | xargs perl -pi -e 's/\”/\"/g';
#   find . -type f | xargs perl -pi -e "s/\’/\'/g";
# }

alias dup=". ~/.dotfiles/bootstrap.sh"
alias brup="brew update && brew upgrade"
alias sup="svn up ~/sandbox/svn-repos/*; svn status ~/sandbox/svn-repos/*"
alias gup="gg ~/sandbox/git-repos/*"
alias up="echo '### Pulling git-repos/* ###'; gup; echo '### Pulling/Status svn-repos/* ###'; sup; echo '### Pulling ~/.dotfiles ###'; dup; echo '### Brew Up ###'; brup"
alias gst="gs ~/sandbox/git-repos/*"
alias clean='find . -name "*.pyc" -delete; find . -name ".DS_Store" -delete'
alias smush='python ~/sandbox/git-repos/smush.py/smush/smush.py -r -s'

alias resetaudio='sudo killall coreaudiod'

alias c="clear"


